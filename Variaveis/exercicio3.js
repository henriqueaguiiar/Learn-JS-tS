//Declare duas variáveis, uma contendo um número e outra contendo uma string. Em seguida, combine-as em uma terceira variável usando 
// template strings para montar uma frase e exiba o resultado no console.

let nome = "Henrique";
let numero = 25;

let frase = `Nome: ${nome} e Idade: ${numero}`;
console.log(frase);